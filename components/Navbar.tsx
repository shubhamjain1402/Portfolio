'use client';
import {useEffect,useState} from 'react';
import {Moon,Sun} from 'lucide-react';
import {portfolio} from '@/data/portfolio';
export default function Navbar(){const [scrolled,setScrolled]=useState(false);const [dark,setDark]=useState(false);const [active,setActive]=useState('');useEffect(()=>{const change=()=>setScrolled(window.scrollY>30);change();window.addEventListener('scroll',change,{passive:true});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)setActive('#'+entry.target.id)}),{rootMargin:'-15% 0px -60% 0px'});document.querySelectorAll('main section[id]').forEach(el=>observer.observe(el));return()=>{window.removeEventListener('scroll',change);observer.disconnect()}},[]);return <header className={`navbar ${scrolled?'scrolled':''}`}><a className="wordmark" href="#top" aria-label={`${portfolio.name} home`}>{portfolio.initials}<span> / {portfolio.id}</span></a><nav aria-label="Main navigation">{portfolio.navigation.map(link=><a key={link.href} href={link.href} aria-current={active===link.href?'location':undefined}>{link.label}</a>)}</nav><button className="theme-toggle" aria-label={`Switch to ${dark?'light':'dark'} mode`} onClick={()=>{document.documentElement.dataset.theme=dark?'light':'dark';setDark(!dark)}}>{dark?<Sun size={18}/>:<Moon size={18}/>}</button></header>}
