'use client';
import Image from 'next/image';
import {motion,useMotionValue,useSpring,useReducedMotion} from 'framer-motion';
import {BrainCircuit,ScanLine,Database,Workflow,Code2} from 'lucide-react';
import {portfolio} from '@/data/portfolio';
const icons=[BrainCircuit,ScanLine,Database,Workflow];
export default function IdentityCard(){const x=useMotionValue(0),y=useMotionValue(0);const rotateX=useSpring(x),rotateY=useSpring(y);const reduced=useReducedMotion();return <div className="badge-stage"><div className="lanyard" aria-hidden="true"/><motion.aside className="identity-card" aria-label="Portfolio identity" style={{rotateX,rotateY}} onPointerMove={event=>{if(reduced||event.pointerType==='touch')return;const box=event.currentTarget.getBoundingClientRect();x.set(-(event.clientY-box.top-box.height/2)/100);y.set((event.clientX-box.left-box.width/2)/100)}} onPointerLeave={()=>{x.set(0);y.set(0)}}><div className="badge-clip" aria-hidden="true"/><div className="id-header"><span className="eyebrow">{portfolio.labels.portfolioId}</span><strong>{portfolio.id}</strong></div><div className="identity-main"><div className="portrait">{portfolio.portrait?<Image src={portfolio.portrait} alt={`${portfolio.name} portrait`} fill sizes="180px" priority/>:<span aria-label={`${portfolio.name} monogram`}>{portfolio.initials}<small>{portfolio.id}</small></span>}</div><div><span className="eyebrow">{portfolio.labels.name}</span><h2>{portfolio.name}</h2><span className="eyebrow access-label">{portfolio.labels.access}</span><div className="tags access">{portfolio.access.map(tag=><span key={tag}>{tag}</span>)}</div></div></div><div className="capabilities">{portfolio.capabilities.map((label,i)=>{const Icon=icons[i];return <div key={label}><Icon size={18} aria-hidden="true"/>{label}</div>})}</div><div className="id-bottom"><span><Code2 size={15}/>{portfolio.status}</span><a href="#projects">{portfolio.hero.scroll} ↓</a></div></motion.aside></div>}
